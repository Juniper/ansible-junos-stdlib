---
- name: Junos OS version
  hosts: all
  connection: local
  gather_facts: no

  tasks:
    - name: Get Junos Facts
      junos_get_facts:
        host={{ inventory_hostname }}
        user={{ admin_user }}
      register: junos
    - name: version
      debug: msg="{{ junos.facts.version }}"
